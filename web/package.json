{
  "name": "admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm run -r dev",
    "build": "pnpm run -r dev build",
    "preview": "pnpm run -r dev preview"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.4.0",
    "@types/node": "^18.11.9",
    "rome": "^10.0.0",
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  },
  "dependencies": {
    "preact": "^10.11.2",
    "wouter-preact": "^2.8.1"
  }
}
